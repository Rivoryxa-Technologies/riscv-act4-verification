# Contributing

Keep revisions pinned by full commit hash and preserve upstream attribution.
Run `python3 -m unittest discover -s tests -v` after changing the wrapper. A
matrix change must name the exact DUT configuration and expected test count;
never describe a subset or integration run as general compliance.
